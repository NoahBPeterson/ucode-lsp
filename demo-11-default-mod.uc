// Module for demo-11-default-specifier.uc — exports a function as the default.
function answer() { return 42; }
export { answer as default };       // `x as default` — valid on all ucode versions // colored blue, unlike anything else...? // no hover on `default`



